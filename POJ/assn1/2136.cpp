#include <iostream>
#include <string>
#include <vector>
#include <map>

using namespace std;

vector<int> countAlpha(string a);
void printList(vector<char> &vec, int max_freq) {

}

int main() {
    string a, b, c, d;
    getline(cin, a);
    vector<char> alphabets(26);
    for (char c : a) {
        alphabets.push_back(c);
    }
    
}
